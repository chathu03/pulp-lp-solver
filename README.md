# Linear Programming Solver (PuLP)

A dynamic Python script that allows users to input objective functions and constraints to solve Linear Programming problems (Maximization or Minimization).

## Prerequisites
You will need the `pulp` library installed:
```bash
pip install pulp
